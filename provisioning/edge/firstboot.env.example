# Example configuration for ovr-firstboot (copy to /etc/ovr/firstboot.env or similar)
# NOTE: Do not commit real secrets to git. Use *_FILE paths that point to /etc/ovr/secrets/*.

LAN_MODE=dhcp
# WIFI_SSID="Your SSID"
# WIFI_PASS_FILE=/etc/ovr/secrets/wifi_password

# Expose only what you need
ALLOWED_TCP_PORTS=22,3000,8428,8429,9100,8080,8088

LOG_FILE=/var/log/ovr-firstboot.log

# Remote connectivity probes
DNS_PROBE_HOST=deb.debian.org
IP_PROBE_HOST=1.1.1.1
IP_PROBE_PORT=443

# Stack integration
EVENT_API_KEY_FILE=/etc/ovr/secrets/event_api_key
GX_PASSWORD_FILE=/etc/ovr/secrets/gx_password

# Local VictoriaMetrics endpoints
VM_WRITE_URL=http://victoria-metrics:8428/write
VM_QUERY_URL=http://victoria-metrics:8428

# Optional: secondary cloud write endpoint
# VM_WRITE_URL_SECONDARY=https://metrics.example.com/write
VM_WRITE_USERNAME=ovr
VM_WRITE_PASSWORD_FILE=/etc/ovr/secrets/vm_write_password
