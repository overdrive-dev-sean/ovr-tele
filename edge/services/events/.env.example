# Event Service Environment Variables

# VictoriaMetrics write endpoint (Influx line protocol)
# Default: http://victoria-metrics:8428/write (Docker internal network)
VM_WRITE_URL=http://victoria-metrics:8428/write

# SQLite database path for state persistence
# Default: /data/events.db
DB_PATH=/data/events.db

# Logging level: DEBUG, INFO, WARNING, ERROR
# Default: INFO
LOG_LEVEL=INFO

# Optional API key for authentication
# If set, clients must include "X-API-Key: <value>" header
# Leave empty for no authentication (suitable for internal/isolated networks)
# Default: (empty)
EVENT_API_KEY=

# Service port (internal to container, exposed via docker-compose)
# Default: 8088
PORT=8088

# Map tile providers + guardrails
# Public tokens for browser tile access
MAPBOX_TOKEN=
MAPBOX_TOKEN_FILE=
ESRI_TOKEN=

# Optional overrides for tile URLs/attribution
# MAPBOX_TILE_URL=https://api.mapbox.com/styles/v1/mapbox/satellite-v9/tiles/256/{z}/{x}/{y}?access_token={token}
# ESRI_TILE_URL=https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}
# MAPBOX_ATTRIBUTION=Imagery (c) Mapbox
# ESRI_ATTRIBUTION=Tiles (c) Esri

# Free tier guardrails (UTC month)
MAPBOX_FREE_TILES_PER_MONTH=750000
ESRI_FREE_TILES_PER_MONTH=2000000
GUARDRAIL_LIMIT_PCT=0.95

# Cloud fleet API base URL (optional, for tile usage sync)
# Event sync now happens via MQTT (bidirectional bridge)
CLOUD_API_URL=https://map.example.com
